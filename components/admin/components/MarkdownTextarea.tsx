// Re-export MarkdownPreviewEditor as MarkdownTextarea for backward compatibility
// This component now shows preview by default instead of Markdown source
export { default } from './MarkdownPreviewEditor';

